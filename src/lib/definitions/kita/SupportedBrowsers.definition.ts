/**
 * Browsers that are currently supported by the WebDriver
 * @enum {string}
 * @see <>
 */
export const SupportedBrowsers: { [key: string]: string } = {
    CHROME: `chrome`,
    EDGE: `msedge`
};