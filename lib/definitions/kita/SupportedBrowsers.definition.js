'use strict'

/**
 * Browsers that are currently supported by the WebDriver
 * @enum {string}
 * @see <>
 */
const SupportedBrowsers = {
    CHROME: 'chrome',
    EDGE: 'msedge'
}

module.exports = {
    SupportedBrowsers
}