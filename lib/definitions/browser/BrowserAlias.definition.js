'use strict'

/**
 * Names that browsers can be called
 * @enum {string[]}
 * @see <>
 */
const BrowserAlias = {
    CHROME: ['chrome', 'google', 'google chrome'],
    EDGE: ['msedge', 'edge', 'ms', 'microsoft edge'],
    FIREFOX: ['firefox', 'mozilla', 'mozilla firefox'],
    IE: ['ie', 'internet explorer', 'deprecated browser', 'why'],
}

module.exports = {
    BrowserAlias
}
