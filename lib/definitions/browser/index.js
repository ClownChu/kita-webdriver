'use strict'

const { BrowserAlias } = require('./BrowserAlias.definition')
const { BrowserDisplayName } = require('./BrowserDisplayName.definition')
const { SupportedBrowsers } = require('../kita/SupportedBrowsers.definition')

module.exports = {
    BrowserAlias,
    BrowserDisplayName,
    SupportedBrowsers,
}

